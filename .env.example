# Server port
PORT=8080
# development | production
NODE_ENV=development
# Comma-separated list of allowed origins for CORS (Express + Socket.IO)
# In dev, keep localhost/127.0.0.1 with your chosen port
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,https://app-turnos-2025.onrender.com
# In production, set your domain(s), e.g.:
# CORS_ORIGINS=https://turnos.example.com,https://admin.turnos.example.com